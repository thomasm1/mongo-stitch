export class Book {

    id: number;
    isbn: string;
    title: string;

    constructor(id: number, isbn: string, title: string) {
        this.id = id;
        this.isbn = isbn;
        this.title = title;
    }

}